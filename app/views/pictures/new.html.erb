<!DOCTYPE HTML>
<html>
  <head>
    <meta charset="UTF-8">
    <meta link="style.css" rel="stylesheets" type="text/css">
    <meta rel="fonts" >
  </head>
  <body>
  <div class="postView">
    <div class="container">
      <div class="newMainBox">
        <div class="newSubBox1">
          <h2><i class="glyphicon glyphicon-bell"></i>投稿ページ</h2>
        </div>
        <hr>
        <%= form_for [@picture] do |f| %>
        <div class="postPic">
          <ul>
            <li class="picText"><%= f.label :写真 %></li>
            <li class="pic"><%= f.file_field :image %></li>
            <li class="text"><%= f.label :コメント %></li>
            <p class="comment"><%= f.text_area :comment, placeholder: "コメントや服の詳細を書いてね！
(例) アウター: ユニクロ", size: "55x10" %></p>
          </ul>
        </div>
        <div class="submit">
          <%= f.submit "投稿する" %><i class="glyphicon glyphicon-pencil"></i>
        </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
